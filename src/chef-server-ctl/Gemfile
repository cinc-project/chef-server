source "https://rubygems.org"

gemspec

gem "chef", "~> 16.17", source: "https://packagecloud.io/cinc-project/stable"
gem "toml" # for habitat-land

# Cinc wordmark fixes
gem "chef_backup", source: "https://packagecloud.io/cinc-project/stable"
gem "mixlib-install", source: "https://packagecloud.io/cinc-project/stable"

# Version lock these to match Gemfile.lock when doing a stable release
if ENV['CHANNEL'] == 'stable'
  gem "chef-vault", "= 4.1.5"
  gem "ed25519", "= 1.3.0"
  gem "inspec-core", "= 4.52.9", source: "https://packagecloud.io/cinc-project/stable"
  gem "ohai", "= 16.17.0"
  gem "train-core", "= 3.8.6"
else
  gem "inspec-core", source: "https://packagecloud.io/cinc-project/stable"
end
